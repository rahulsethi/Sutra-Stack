{
  "name": "@sutra/mcp",
  "version": "1.0.0",
  "description": "Sutra MCP server — stdio JSON-RPC 2.0, 14 gated tools. Hand-rolled protocol, zero external dependencies.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "bin": {
    "sutra-mcp": "./dist/bin.js"
  },
  "files": ["dist", "README.md"],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@sutra/core": "1.0.0"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "node --test dist"
  }
}
