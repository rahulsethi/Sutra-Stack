{
  "name": "@sutra/cli",
  "version": "1.0.0",
  "description": "The `sutra` command line. Every read supports --json; the exit codes are a governance signal.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "sutra": "./dist/bin.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@sutra/aatma-core": "1.0.0",
    "@sutra/hermes-mcp": "1.0.0"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "node --test dist"
  },
  "sutra": {
    "layer": "hermes"
  }
}
